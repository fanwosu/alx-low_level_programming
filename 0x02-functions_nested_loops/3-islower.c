#include "main.h"
/**
 * _islower -  is a function that checks for lowercase
 *
 * @x: this is the character to check
 *
 * Return: returns 1 if lowercase and 0 if not lowercase
*/
int _islower(int x)
{
	return (x >= 'a' && x <= 'z');
}
