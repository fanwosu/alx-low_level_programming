#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @c: is the inte to be checked.
 *
 * Return: this returns the absolute value of an integer
*/

int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	else
	{
		return (-c);
	}
}
